{% extends '::front-office.html.twig' %}
{% block javascripts %}
	{{ parent() }}
	<script type="text/javascript" src="https://js.stripe.com/v2/"></script>
	<script type="text/javascript">
	    Stripe.setPublishableKey('pk_test_bRKL5eJi4tc9TmWOaUtc0y47');
	    var stripeResponseHandler = function(status, response) {
			var $form = $('#payment-form');
			if (response.error) {
	        	$form.find('.payment-errors').text(response.error.message);
	        	$form.find('button').prop('disabled', false);
	      	} else {
	        	var token = response.id;
	        	// Insert the token into the form so it gets submitted to the server
	        	$form.append($('<input type="hidden" name="stripeToken" />').val(token));
	        	$form.get(0).submit();
	      	}
	    };
	    
	    $(document).on("ready", function() {
			$('#payment-form').on("submit", function(event) {
		        event.preventDefault();
		        var $form = $(this);
		        $form.find('button').prop('disabled', true);
		        Stripe.card.createToken($form, stripeResponseHandler);
	      	});
	    });
  	</script>
{% endblock %}
{% block content %}
<h2>Pour 12,99€ nous nous occupons de tout.</h2>
<p class="text-danger">Nous rédigeons et envoyons par recommandé votre lettre de résiliation à votre fournisseur.<br /><br />
Vous recevrez un email de confirmation et un accusé de réception de La Poste.<br /><br />
Votre contrat apparaîtra alors automatiquement dans votre dashboard comme "Résilié".
</p>
<p class="text-center text-danger">Entrez vos informations de paiement ci-dessous pour commander votre résiliation.</p>
<form action="" method="POST" id="payment-form">
    <span class="payment-errors"></span>
    <div class="form-row">
		<label>N° de carte: </label>
    	<input type="text" size="16" data-stripe="number"/>
    </div>
    <div class="form-row">
      	<label>Clé de sécurité: </label>
        	<input type="text" size="4" data-stripe="cvc"/>
  	</div>
    <div class="form-row">
      	<label>Date d'expiration (MM/YYYY): </label>
    	<input type="text" size="2" data-stripe="exp-month"/>
  		<span> / </span>
      	<input type="text" size="4" data-stripe="exp-year"/>
    </div>
    <button type="submit" class="btn btn-danger">Payer</button>
</form>
{% endblock %}