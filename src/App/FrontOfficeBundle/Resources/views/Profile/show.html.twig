{% extends 'AppFrontOfficeBundle::layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-info">
                <div class="panel-body">
                	<div class="col-md-6">
	                	<h2>Mes données personnelles</h2>
	                    <p>{{ app.user.gender }} {{ app.user.firstname }} {{ app.user.lastname }}</p>
	                    <p>{{ app.user.email }}</p>
	                    <p>{{ app.user.phone }}</p>
	                	<h2>Adresse</h2>
	                    <p>{{ app.user.address }}</p>
	                    <p>{{ app.user.zipCode }} {{ app.user.city }}</p>
	                    <p>{{ app.user.country }}</p>
                    </div>
                	<div class="col-md-6">
	                	<h2>Ma photo de profil</h2>
	                    <p class="clearfix"><img src="{{ app.user.avatarWebPath }}" class="col-md-6 center-block" alt="Photo de profil" /></p>
	                    <a href="{{ path("fos_user_profile_edit") }}" class="btn btn-danger center-block clearfix col-md-6">Modifier</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}