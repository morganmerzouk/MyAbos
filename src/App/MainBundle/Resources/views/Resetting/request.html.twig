<div class="bg-danger hide"></div>
<form method="POST" class="fos_user_resetting_request">
    <div class="form-group">
        <label for="username">{{ 'resetting.request.username'|trans({}, 'FOSUserBundle') }}</label>
        <input type="text" id="username" class="form-control" name="username" required="required" />
    </div>
    <a class="btn btn-primary" href="javascript:showSendMailForm('{{ path('fos_user_resetting_send_email') }}');">{{ 'resetting.request.submit'|trans({}, 'FOSUserBundle') }}</a>
</form>