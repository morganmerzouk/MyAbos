{% extends "AppBundle:Front:OffreSpeciale/offrespeciale.html.twig" %}
{% block content %}
    {{ parent() }}
    {% if skipper is not null %}
	    <div class="col-md-12">
	    	<h2>Captain {{ skipper.name }}</h2>
	    	{{ skipper.description|raw }}
		</div>
    {% endif %}
{% endblock content %}